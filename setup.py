from setuptools import setup, find_packages

setup(name='localized_smoothing',
      version='0.1.0',
      description='localized smoothing for collective robustness',
      packages=find_packages(),
      zip_safe=False)
